/* Paste your deployed Apps Script Web App URL between the quotes (the .../exec URL).
   One backend serves the "?" question/comment button across every lecture deck.
   See backend.gs in this folder for setup steps. Leave blank and the button will
   tell students the form isn't connected yet, instead of failing silently. */
window.QUESTIONS_API = "";
